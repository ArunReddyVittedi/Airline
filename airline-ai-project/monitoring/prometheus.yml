global:
  # Short local scrape interval for responsive dashboard updates.
  scrape_interval: 5s

scrape_configs:
  - job_name: airline-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - host.docker.internal:8080
